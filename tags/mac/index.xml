<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>
             Mac on Vicky&#39;s Blog 
        </title>

        <link>https://vicky335.github.io/tags/mac.html</link>

        
        <author>vicky335 (https://vicky335.github.io/)</author>
        

        
        <managingEditor>vicky335 (https://vicky335.github.io/)</managingEditor>
        

        
        <webMaster>vicky335 (https://vicky335.github.io/)</webMaster>
        

        <description>
            This is vicky335&#39;s blog
        </description>

        
        <language>zh-Hant</language>
        

        <copyright>
            <a href="https://vicky335.github.io/">Vicky&#39;s Blog</a> All rights reserved - 2017
        </copyright>

        
        <lastBuildDate>Mon, 28 May 2018 15:00:21 UTC+08:00</lastBuildDate>
        

        <atom:link href="https://vicky335.github.io/tags/mac.html" rel="self" type="application/rss+xml" />

        
        <item>
            <title>Mac 常用快捷鍵整理</title>

            <link>https://vicky335.github.io/post/other/mac_shortcutkeys.html</link>

            <pubDate>Mon, 28 May 2018 15:00:21 UTC+08:00</pubDate>

            
            <author>vicky335 (https://vicky335.github.io/)</author>
            

            <guid>https://vicky335.github.io/post/other/mac_shortcutkeys.html</guid>

            <description>文本編輯 以下適用於文本編輯器，瀏覽器等： 跳到頁首 cmd &#43; ↑ 類似windows下的 ctrl &#43; home 跳到頁尾 cmd &#43; ↓ 類似windows下的 ctrl &#43; end 光標移到行首 cmd &#43;</description>
            <content:encoded>
                
                &lt;![CDATA[

<h2 id="文本編輯">文本編輯</h2>

<p>以下適用於文本編輯器，瀏覽器等：</p>

<p>跳到頁首  <code>cmd</code> + <code>↑</code>  類似windows下的 <code>ctrl + home</code></p>

<p>跳到頁尾  <code>cmd</code> + <code>↓</code>  類似windows下的 <code>ctrl + end</code></p>

<p>光標移到行首  <code>cmd</code> + <code>←</code>  類似windows下的 <code>home</code></p>

<p>光標移到行尾  <code>cmd</code> + <code>→</code>  類似windows下的 <code>end</code></p>

<p>選擇一行  <code>cmd</code> + <code>shift</code> + <code>→</code> 或 <code>cmd</code> + <code>shift</code> + <code>→</code></p>

<p>選擇一個單詞  <code>option</code> + <code>shift</code> + <code>←</code> 或 <code>option</code> + <code>shift</code> + <code>→</code></p>

<p>向上滾一屏 <code>fn</code> + <code>↑</code></p>

<p>向下滾一屏 <code>fn</code> + <code>↓</code></p>

<p>刪除一整行文字  <code>cmd + delete</code></p>

<p>刪除一個單詞    <code>option + delete</code></p>

<p>光標移動一個單詞 <code>option + ←</code> 或者 <code>option + →</code></p>

<p>##截屏</p>

<p>全屏截圖自動保存為文件 <code>cmd + shift + 3</code></p>

<p>區域截圖自動保存為文件 <code>cmd + shift + 4</code>，這裡有個小技巧，按下<code>cmd + shift + 4</code>後單擊空格鍵，會出現一個相機的icon，表示自動捕捉這個區域。</p>

<p>##窗口</p>

<p>關閉當前窗口  <code>cmd + w</code></p>

<p>退出當前程序  <code>cmd + q</code></p>

<p>最小化窗口  <code>cmd + m</code></p>

<p>切換窗口程序  <code>cmd + tab</code></p>

<p>快速顯示桌面  <code>cmd + F3</code></p>

<p>顯示當前程序縮略圖  <code>ctl + F3</code></p>

<p>顯示桌面和縮略圖  <code>F3</code></p>

<p>顯示Dashboard  <code>F4</code></p>

<h2 id="terminal-終端">Terminal 終端</h2>

<p>Mac不像<code>Ubuntu</code>可以使用 Ctrl+T 快捷鍵呼出終端，但是可以通過<code>control+space</code>呼出<code>Spotlight</code>，然後輸入terminal回車快速打開終端。</p>

<p>光標移到行首 <code>ctrl</code> + <code>A</code></p>

<p>光標移到行尾  <code>ctrl</code> + <code>E</code></p>

<p>清除一行  <code>ctrl</code> + <code>U</code></p>

<p>撤銷清除  <code>ctrl</code> + <code>Y</code></p>

<p>清屏  <code>ctrl</code> + <code>L</code></p>

<p>新建一個Tab  <code>cmd</code> + <code>T</code></p>

<p>關閉一個Tab  <code>cmd</code> + <code>W</code></p>

<h2 id="文件相關">文件相關</h2>

<pre><code># 創建目錄
$ mkdir newDir
# 刪除目錄及子目錄中的內容, 慎用!!
$ rm -rf newDir
# 複製
$ cp file toDir
# 設置目錄讀寫權限
$ sudo chmod 777 /etc/hosts

</code></pre>

<h2 id="進程相關">進程相關</h2>

<p>mac os x 下殺死進程</p>

<pre><code># 找到要殺死進程的id
$ ps -ef | grep idea
# 強制kill掉進程
$ kill 91107
# 根據項目找對應的進程id
$ ps -ef | grep m-web
$ lsof -i:55010

$ lsof -i:55010
COMMAND  PID    USER   FD   TYPE             DEVICE SIZE/OFF NODE NAME
java    6227 fanyong    9u  IPv4 0xf526b9f45ed7c075      0t0  TCP *:55010 (LISTEN)
$ lsof -i:9001
COMMAND  PID    USER   FD   TYPE             DEVICE SIZE/OFF NODE NAME
java    6227 fanyong  254u  IPv6 0xf526b9f456b4bcf5      0t0  TCP *:etlservicemgr (LISTEN)

</code></pre>

<p><code>cmd + opt + esc</code> ，打開類似windows下任務管理器的快捷鍵。</p>

<p><img src="https://vicky335.github.io/images/other/kill-window.jpg" alt="" /></p>

<h2 id="網絡相關">網絡相關</h2>

<pre><code># 查看本機IP、Mac地址等信息
$ ifconfig en0
# 查看nameserver 修改 nameserver 172.19.8.1 -&gt; nameserver 172.19.8.2
$ sudo vim /etc/resolv.conf
# 診斷
$ dig lujs.cn
$ nslookup lujs.cn

</code></pre>

<h2 id="chrome-快捷鍵">chrome 快捷鍵</h2>

<p>新建一個Tab  <code>cmd</code> + <code>T</code></p>

<p>關閉一個Tab  <code>cmd</code> + <code>W</code></p>

<p>打開開發者工具，<code>cmd</code> + <code>option</code> + <code>I</code> Mac下F12不管用,因為系統默認把<code>F12</code>分配給了音量</p>

<p>查看源代碼  <code>cmd</code> + <code>option</code> + <code>U</code></p>

<p>調試js - step over <code>cmd</code> + <code>\</code></p>

<p>調試js - step in <code>cmd</code> + <code>;</code></p>

<p>調試js - step out <code>cmd</code> + <code>'</code></p>

<p>快速定位Tab  <code>cmd</code> + <code>1/2/3/等</code>
向左切換Tab順序  <code>cmd</code> + <code>shift</code> + <code>{</code></p>

<p>向右切換Tab順序  <code>cmd</code> + <code>shift</code> + <code>}</code></p>
]]>
            </content:encoded>

        </item>
        
    </channel>
</rss>
