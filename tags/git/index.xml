<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>
             Git on Vicky&#39;s Blog 
        </title>

        <link>https://vicky335.github.io/tags/git.html</link>

        
        <author>vicky335 (https://vicky335.github.io/)</author>
        

        
        <managingEditor>vicky335 (https://vicky335.github.io/)</managingEditor>
        

        
        <webMaster>vicky335 (https://vicky335.github.io/)</webMaster>
        

        <description>
            This is vicky335&#39;s blog
        </description>

        
        <language>zh-Hant</language>
        

        <copyright>
            <a href="https://vicky335.github.io/">Vicky&#39;s Blog</a> All rights reserved - 2017
        </copyright>

        
        <lastBuildDate>Tue, 14 Apr 2020 10:21:24 UTC+08:00</lastBuildDate>
        

        <atom:link href="https://vicky335.github.io/tags/git.html" rel="self" type="application/rss+xml" />

        
        <item>
            <title>Git常用命令清單</title>

            <link>https://vicky335.github.io/post/f2e/git.html</link>

            <pubDate>Tue, 14 Apr 2020 10:21:24 UTC+08:00</pubDate>

            
            <author>vicky335 (https://vicky335.github.io/)</author>
            

            <guid>https://vicky335.github.io/post/f2e/git.html</guid>

            <description>新建代碼庫 # 在當前目錄新建一個Git代碼庫 $ git init # 新建一個目錄，將其初始化為Git代碼庫 $ git init [project-name] # 下載一個項目和它的整個代碼歷史 $ git clone [url] git c</description>
            
            <content:encoded>
                <![CDATA[

<h2 id="新建代碼庫">新建代碼庫</h2>

<pre><code># 在當前目錄新建一個Git代碼庫
$ git init

# 新建一個目錄，將其初始化為Git代碼庫
$ git init [project-name]

# 下載一個項目和它的整個代碼歷史
$ git clone [url]

</code></pre>

<ul>
<li><p>git clone：當你已經有一個遠程的Git版本庫，只需要在本地克隆一份，例如：
<code>git clone git://github.com/someone/some_project.git some_project</code>
上面的命令就是將<code>git://github.com/someone/some_project.git</code>這個URL地址的遠程版本庫，完全克隆到本地<code>some_project</code>目錄下</p></li>

<li><p>git init 和 git remote：當你本地創建了一個工作目錄，你可以進入這個目錄，使用&rsquo;git init&rsquo;命令進行初始化；Git以後就會對該目錄下的文件進行版本控制，這時候如果你需要將它放到遠程服務器上，可以在遠程服務器上創建一個目錄，並把可訪問的URL記錄下來，此時你就可以利用&rsquo;git remote add&rsquo;命令來增加一個遠程服務器端，例如：
<code>git remote add origin git://github.com/someone/another_project.git</code>
上面的命令就會增加URL地址為<code>git://github.com/someone/another_project.git</code>，名稱為<code>origin</code>的遠程服務器，以後提交代碼的時候只需要使用<code>origin</code>別名即可</p></li>
</ul>

<h2 id="配置">配置</h2>

<p>Git的設置文件為<code>.gitconfig</code>，它可以在用戶主目錄下（全局配置），也可以在項目目錄下（項目配置）。</p>

<pre><code># 顯示當前的Git配置
$ git config --list

# 編輯Git配置文件
$ git config -e [--global]

# 設置提交代碼時的用戶信息
$ git config [--global] user.name &quot;[name]&quot;
$ git config [--global] user.email &quot;[email address]&quot;
</code></pre>

<h2 id="忽略一些文件-文件夾不提交">忽略一些文件、文件夾不提交</h2>

<p>在倉庫根目錄下創建名稱為<code>.gitignore</code>的文件，寫入不需要的文件夾名或文件，每個元素佔一行即可，如</p>

<pre><code>target

bin

*.db
</code></pre>

<h2 id="增加-刪除文件">增加/刪除文件</h2>

<pre><code># 添加指定文件到暫存區
$ git add [file1] [file2] ...

# 添加指定目錄到暫存區，包括子目錄
$ git add [dir]

# 添加當前目錄的所有文件到暫存區
$ git add .

# 添加每個變化前，都會要求確認
# 對於同一個文件的多處變化，可以實現分次提交
$ git add -p

# 刪除工作區文件，並且將這次刪除放入暫存區
$ git rm [file1] [file2] ...

# 停止追蹤指定文件，但該文件會保留在工作區
$ git rm --cached [file]

# 改名文件，並且將這個改名放入暫存區
$ git mv [file-original] [file-renamed]
</code></pre>

<h2 id="代碼提交">代碼提交</h2>

<pre><code># 提交暫存區到倉庫區
$ git commit -m [message]

# 提交暫存區的指定文件到倉庫區
$ git commit [file1] [file2] ... -m [message]

# 提交工作區自上次commit之後的變化，直接到倉庫區
$ git commit -a

# 提交時顯示所有diff信息
$ git commit -v

# 使用一次新的commit，替代上一次提交
# 如果代碼沒有任何新變化，則用來改寫上一次commit的提交信息
$ git commit --amend -m [message]

# 重做上一次commit，並包括指定文件的新變化
$ git commit --amend [file1] [file2] ...
</code></pre>

<h2 id="分支">分支</h2>

<pre><code># 列出所有本地分支
$ git branch

# 列出所有遠程分支
$ git branch -r

# 列出所有本地分支和遠程分支
$ git branch -a

# 新建一個分支，但依然停留在當前分支
$ git branch [branch-name]

# 新建一個分支，並切換到該分支
$ git checkout -b [branch]

# 新建一個分支，指向指定commit
$ git branch [branch] [commit]

# 新建一個分支，與指定的遠程分支建立追蹤關係
$ git branch --track [branch] [remote-branch]

# 切換到指定分支，並更新工作區
$ git checkout [branch-name]

# 切換到上一個分支
$ git checkout -

# 建立追蹤關係，在現有分支與指定的遠程分支之間
$ git branch --set-upstream [branch] [remote-branch]

# 合併指定分支到當前分支
$ git merge [branch]

# 選擇一個commit，合併進當前分支
$ git cherry-pick [commit]

# 刪除分支
$ git branch -d [branch-name]

# 刪除遠程分支
$ git push origin --delete [branch-name]
$ git branch -dr [remote/branch]
</code></pre>

<h2 id="標籤-tag">標籤(tag)</h2>

<pre><code># 列出所有tag
$ git tag

# 新建一個tag在當前commit
$ git tag [tag]

# 新建一個tag在指定commit
$ git tag [tag] [commit]

# 刪除本地tag
$ git tag -d [tag]

# 刪除遠程tag
$ git push origin :refs/tags/[tagName]

# 查看tag信息
$ git show [tag]

# 提交指定tag
$ git push [remote] [tag]

# 提交所有tag
$ git push [remote] --tags

# 新建一個分支，指向某個tag
$ git checkout -b [branch] [tag]
</code></pre>

<h2 id="子模塊-submodule">子模塊(submodule)</h2>

<pre><code># 添加子模塊
$ git submodule add [url] [path]

# 初始化子模塊，只在首次檢出倉庫時運行一次就行
$ git submodule init

# 更新子模塊，每次更新或切換分支後都需要運行一下
$ git submodule update 

# 刪除子模塊(分4步)
- $ git rm --cached [path]
- 編輯 .gitmodules 文件，將子模塊的相關配置節點刪除掉
- 編輯 .git/config 文件，將子模塊的相關配置節點刪除掉
- 手動刪除子模塊殘留的目錄
</code></pre>

<h2 id="查看信息">查看信息</h2>

<pre><code># 顯示有變更的文件
$ git status

# 顯示當前分支的版本歷史
$ git log

# 顯示commit歷史，以及每次commit發生變更的文件
$ git log --stat

# 搜索提交歷史，根據關鍵詞
$ git log -S [keyword]

# 顯示某個commit之後的所有變動，每個commit佔據一行
$ git log [tag] HEAD --pretty=format:%s

# 顯示某個commit之後的所有變動，其&quot;提交說明&quot;必須符合搜索條件
$ git log [tag] HEAD --grep feature

# 顯示某個文件的版本歷史，包括文件改名
$ git log --follow [file]
$ git whatchanged [file]

# 顯示指定文件相關的每一次diff
$ git log -p [file]

# 顯示過去5次提交
$ git log -5 --pretty --oneline

# 顯示所有提交過的用戶，按提交次數排序
$ git shortlog -sn

# 顯示指定文件是什麼人在什麼時間修改過
$ git blame [file]

# 顯示暫存區和工作區的差異
$ git diff

# 顯示暫存區和上一個commit的差異
$ git diff --cached [file]

# 顯示工作區與當前分支最新commit之間的差異
$ git diff HEAD

# 顯示兩次提交之間的差異
$ git diff [first-branch]...[second-branch]

# 顯示今天你寫了多少行代碼
$ git diff --shortstat &quot;@{0 day ago}&quot;

# 顯示某次提交的元數據和內容變化
$ git show [commit]

# 顯示某次提交發生變化的文件
$ git show --name-only [commit]

# 顯示某次提交時，某個文件的內容
$ git show [commit]:[filename]

# 顯示當前分支的最近幾次提交
$ git reflog
</code></pre>

<h2 id="遠程同步">遠程同步</h2>

<pre><code># 下載遠程倉庫的所有變動
$ git fetch [remote]

# 顯示所有遠程倉庫
$ git remote -v

# 顯示某個遠程倉庫的信息
$ git remote show [remote]

# 增加一個新的遠程倉庫，並命名
$ git remote add [shortname] [url]

# 取回遠程倉庫的變化，並與本地分支合併
$ git pull [remote] [branch]

# 上傳本地指定分支到遠程倉庫
$ git push [remote] [branch]

# 強行推送當前分支到遠程倉庫，即使有衝突
$ git push [remote] --force

# 推送所有分支到遠程倉庫
$ git push [remote] --all
</code></pre>

<h2 id="撤銷">撤銷</h2>

<pre><code># 恢復暫存區的指定文件到工作區
$ git checkout [file]

# 恢復某個commit的指定文件到暫存區和工作區
$ git checkout [commit] [file]

# 恢復暫存區的所有文件到工作區
$ git checkout .

# 重置暫存區的指定文件，與上一次commit保持一致，但工作區不變
$ git reset [file]

# 重置暫存區與工作區，與上一次commit保持一致
$ git reset --hard

# 重置當前分支的指針為指定commit，同時重置暫存區，但工作區不變
$ git reset [commit]

# 重置當前分支的HEAD為指定commit，同時重置暫存區和工作區，與指定commit一致
$ git reset --hard [commit]

# 重置當前HEAD為指定commit，但保持暫存區和工作區不變
$ git reset --keep [commit]

# 新建一個commit，用來撤銷指定commit
# 後者的所有變化都將被前者抵消，並且應用到當前分支
$ git revert [commit]

# 暫時將未提交的變化移除，稍後再移入
$ git stash
$ git stash pop
</code></pre>

<h2 id="其他">其他</h2>

<pre><code># 生成一個可供發佈的壓縮包
$ git archive
</code></pre>
]]>
            </content:encoded>
        </item>
        
    </channel>
</rss>
